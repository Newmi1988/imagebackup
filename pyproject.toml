[project]
name = "imagebackup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tobias Newmiwaka", email = "newmi1988@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "flask>=3.1.3",
    "python-json-logger>=4.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]

[project.scripts]
imagebackup = "imagebackup:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
